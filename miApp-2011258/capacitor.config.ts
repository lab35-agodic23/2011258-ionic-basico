import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'miApp-2011258',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
